The schema directory contains the schema files for our ldap service. In there you 
will find .schema files and .ldif files. The LDIF files are LDAP interchange format files, a special format to 
specify data changes in LDAP. In the slapd.d directory you will find the files comprising the SLAPD DIT.