You will still be able to run the original command by specifying the full path to the 
executable, but it might create nasty surprises when you don’t expect them (e.g., in automated scripts).
To delete an alias, use the unalias command.