These are usually packaged, so we’ll simply install them via the package system. Table 8-6 shows the 
required packages for Red Hat and Ubuntu.

Table 8-6.  Installing a Compiler and Essential Build Tools

Distribution

Command

CentOS

Ubuntu

yum install gcc make

aptitude install build-essential

Configure
With the compiler installed, we can now configure the sources for building.